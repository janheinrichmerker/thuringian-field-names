export interface GeoCoordinates {
  /**
   * Latitude between -90° (south) and 90° (north). 
   */
  latitude: number;
  /**
   * Longitude between -180° (west) and 180° (east).
   */
  longitude: number;
}
