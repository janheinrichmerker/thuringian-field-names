/**
 * Type of field name.
 * Can either be a field marking (German: Gemarkung) 
 * or a field name card (German: Flurkarte).
 */
export enum FieldNameType {
  Marking = "marking",
  Card = "card",
}
